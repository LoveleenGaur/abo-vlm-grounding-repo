# Pinned to the published run environment (Python 3.12).
transformers==4.51.3
accelerate==1.6.0
huggingface-hub==0.36.2
torch>=2.0            # only needed when RUN_GENERATION = True
pillow>=10.0
pandas==2.2.2
numpy==2.0.2
scipy>=1.11
matplotlib>=3.7
tqdm>=4.66
requests>=2.31
scikit-learn>=1.3     # only needed for audit/score_audit.py
